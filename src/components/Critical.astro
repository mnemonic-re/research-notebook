---
interface Props {
  title?: string;
}

const { title = "CRITICAL" } = Astro.props;
---

<div class="callout callout-critical">
  <div class="callout-title">
    <span class="callout-icon">⛔</span>
    <span>{title}</span>
  </div>
  <div class="callout-body">
    <slot />
  </div>
</div>

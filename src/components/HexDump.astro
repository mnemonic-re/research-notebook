---
interface Props {
  title?: string;
}

const { title = "HEX / MEMORY DUMP" } = Astro.props;
---

<div class="callout callout-command callout-hexdump">
  <div class="callout-title">
    <span>[{title}]</span>
  </div>
  <div class="callout-content">
    <slot />
  </div>
</div>
